// console.log(window);

// window global variable gives us access to information and functionality to the active brower's tab/window
// console.log(document);

// document global variable:- Like the "window" global variable, this variable gives us access to information and functionality to the
// content displayed on our browser's tab / window. 
// to view the javascript object we use the console.dir() method. as this .dir() method displays an interactive list of the properties
// of the specified Javascript object, and this overrides the .log() method.

console.dir(document);